#![allow(dead_code, unused_variables)]
///
/// brain-teaser-educative-ex-19
///
/// # Commands
///
/// ```RUST_BACKTRACE=full cargo run -q -p brain-teaser-educative_bin --bin brain-teaser-educative-ex-19```
///
/// ```cargo doc  --package brain-teaser-educative_bin --message-format short --no-deps --open --color always```
///
/// ```cargo test --doc  --package brain-teaser-educative_bin```
///
/// ```cargo clippy --package brain-teaser-educative_bin --bin brain-teaser-educative-ex-19```
///
/// ## What
/// `TODO`
///
/// ## How
/// `TODO`
///
/// ## Solution
///
/// //```rust,no_run,compile_fail```
///
/// # Arguments
///
/// * `Arg1` - This is the [your type] to [your verb] the [your struct/func name]
///
/// # Return
/// Starting 1
/// Starting 1
/// Starting 2
/// Returning 2
/// Returning 3
/// Returning 3
///
/// ## Example
/// //```rust,no_run,compile_fail /// ```
///

// use tokio::join;
// use std::time::Duration;

// async fn count_and_wait(n: u64) -> u64 {
//     println!("Starting {}", n);
//     std::thread::sleep(Duration::from_millis(n * 100));
//     println!("Returning {}", n);
//     n
// }

// #[tokio::main]
// async fn main() -> Result<(), Box<dyn std::error::Error>> {
//     // Join runs multiple tasks concurrently and returns when they all
//     // complete execution.
//     join!(count_and_wait(1), count_and_wait(2), count_and_wait(3));
//     Ok(())
// }

fn main(){
    unimplemented!();
}
