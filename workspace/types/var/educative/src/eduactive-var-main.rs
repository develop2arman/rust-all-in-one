#![allow(dead_code, unused_variables)]

/// Main
///
/// ## Commands
///
/// ```cargo run -q -p eduactive-var_bin --bin eduactive-var-main```
///
/// ```cargo doc  --package eduactive-var_bin --message-format short --no-deps --open --color always```
///
/// ```cargo test --doc  --package eduactive-var_bin```
///
/// ## What
// `TODO`
///
/// ## How
/// `TODO`
///
/// # Arguments
///
/// * `Arg1` - This is the [your type] to [your verb] the [your wrapper/func name]
///
/// # Return
/// `assert:true`
///
/// ## Example
/// //```rust,compile_fail,ignore
///
fn main() {
    let apples = 10;
    println!("apples == {}", apples);
    let apples = 20;
    println!("apples == {}", apples);
}
