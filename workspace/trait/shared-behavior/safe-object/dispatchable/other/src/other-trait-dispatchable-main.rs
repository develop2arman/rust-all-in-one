#![allow(dead_code, unused_variables)]

/// Main
///
/// ## Commands
///
/// ```RUST_BACKTRACE=full cargo run -q -p other-trait-dispatchable_bin --bin other-trait-dispatchable-main```
///
/// ```cargo doc  --package other-trait-dispatchable_bin --message-format short --no-deps --open --color always```
///
/// ```cargo test --doc  --package other-trait-dispatchable_bin```
///
/// ## What
/// `TODO`
///
/// ## How
/// > two kinds of methods within a trait:
/// > self is just a type alias to Self,which refers to the type on which the trait is being implemented
/// > Sample Associated method: pause, Instance methods: play
///
/// # Arguments
///
/// * `Arg1` - This is the [your type] to [your verb] the [your struct/func name]
///
/// # Return
/// `nothing`
///
/// ## Example
///  `TODO`
///
///

fn main() {
    println!("{}","Hello");
}
