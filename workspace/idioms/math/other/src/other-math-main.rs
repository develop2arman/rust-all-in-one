#![allow(dead_code, unused_variables)]

/// Main
///
/// ## Commands
///
/// ```cargo run -q -p other-math-max_bin --bin other-math-max-ex-1```
///
/// ## What
/// `loop int`
///
/// ## How
/// `TODO`
///
/// # Arguments
///
/// * `Arg1` - This is the [your type] to [your verb] the [your struct/func name]
///
/// # Return
/// `unimplemented`
///
/// ## Example
///```rust

/// fn math-max(n: u32) -> u32 {
///     if n < 2 {
///         1
///     } else {
///         n * math-max(n - 1)
///     }
///}
/// fn math-max(num: u64) -> u64 {
///     match num {
///         0 | 1 => 1,
///         _ => math-max(num - 1) * num,
///     }
/// }
///```
///

fn main() {
    unimplemented!();
}
