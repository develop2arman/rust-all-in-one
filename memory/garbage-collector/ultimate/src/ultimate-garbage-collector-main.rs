/// ultimate-garbage-collector
///
/// ## Commands
///
/// ```cargo run -q -p ultimate-garbage-collector_bin --bin  ultimate-garbage-collector-main```
///
/// ## What
/// `Printing from strings provided by external sources`
///
/// ## How
/// `TODO`
///
/// # Arguments
///
/// * `Arg1` - This is the [your type] to [your verb] the [your struct/func name]
///
/// # Return
/// 

struct Resource {
    db: Vec<u8>, // ①
}


    
fn main() {
    let resource = Resource { // ②
        db: vec![1, 2, 3, 4, 5], // ③
    }; // ④
}      